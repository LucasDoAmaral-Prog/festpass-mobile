name: festpass
description: FestPass - Aplicativo universitário para descoberta e acesso a eventos.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^6.5.4
  firebase_core: ^4.11.0
  http: ^1.1.0
  firebase_database: ^12.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
